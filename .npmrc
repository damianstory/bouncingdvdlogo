legacy-peer-deps=true
node-linker=hoisted
# Platform settings for native modules
target_platform=linux
target_arch=x64
# Avoid using the pre-gyp binary download system which can cause issues
prefer-dedupe=false
engine-strict=false 